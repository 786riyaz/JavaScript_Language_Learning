string methods
promise related functions
closure
working on indexes in databases
streams
when we can use multi threading

polyfils
stream
closure with setTimeOut
setImmediate
curring
sql qeury for finding 3rd max
mongodb for finding 3rd max
js vs nodejs
MVCC
EC2 with frontend
ORM
cluster vs child processes
web worker vs child processes
socket io
socket broadcast
virtual dom
what can we do if the page takes much time to load first

prototype chaining

First Class Function
First Order Function
Higher Order Function


Block Variable
    {
        var a = 1;
        let b = 2;
        const c = 3;
    }
    console.log(a,b,c)


    
==============================================================================
module.export vs export
default export rename

throttling

array methods
	indexof, find, some, map, filter, flat
	
object methods 
	freeze, seal
	
what is promise with fetch

JSON.stringify Method with parameters